{
  "backendUrl": "",
  "_comment": "Leave empty for auto-detect. Works locally (backend at hostname:3001), via tunnel (same URL for both), and on static hosts (browser mode). Set backendUrl only if frontend and backend are on different servers."
}
